# cooked-skill (planned)

Reusable SKILL/MCP tool: address in → cross-protocol risk profile out, built on
standardized (Messari) subgraph schemas. Extracted from `apps/web/lib/autopsy` once that
lands (build the autopsy engine as a library from hour one — extraction is packaging,
not a rewrite).
